name: flutter_processing
description: "A new Flutter package project."
version: 0.0.1
homepage: https://github.com/kaljitism/flutter_processing

environment:
  sdk: '>=3.2.0-128.0.dev <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  golden_toolkit: ^0.15.0


flutter:

